export const dynamic = 'force-dynamic';

import CreateForm from '@/components/create-form';

export default function page() {
  return (
    <main className='container-real'>
      <CreateForm />
    </main>
  );
}
